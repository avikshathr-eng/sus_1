// Shown on any post moderation has tagged safety_flag = true (abuse,
// stalking, controlling behavior, etc.). This app is peer opinion, not
// crisis support — say so plainly, every time. Deliberately no phone
// numbers here — a short "talk to someone / contact emergency services"
// pointer, not a hotline directory (see the info sheet's "Safety first"
// section in lib/legalText.js for the one place that lives).
export const SAFETY_BANNER = {
  text:
    'If this involves abuse, stalking, threats, or immediate danger, do not rely on sus. ' +
    'Contact someone you trust or local emergency services.',
}
